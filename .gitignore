# Entorno / secretos
.env
.env.*
!.env.example
*.pem
*.key
credentials.json

# Node
node_modules/
npm-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/

# Next.js / Vercel (AI Management)
.next/
out/
build/
.vercel

# Copia de trabajo del prompt que se monta en el contenedor del bot. No se
# versiona: la fuente de verdad es bot/instrucciones.txt, y tener dos copias
# bajo control de versiones garantiza que acaben divergiendo.
/bot/data/

# Logs
# Anclado a la raíz a propósito: sin la barra inicial, este patrón coincide con
# CUALQUIER carpeta llamada "logs" a cualquier profundidad, y llegó a excluir
# del repositorio las rutas del panel (app/(app)/logs y app/api/logs) sin avisar.
/logs/
*.log
pm2.log

# SO / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
